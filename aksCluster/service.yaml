apiVersion: v1
kind: Service
metadata:
  name: wellfed-service
  namespace: wellfed-backend
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: LoadBalancer
